- token=sessid=cookie
- `zeek -r traffic.pcap script.zeek` to run a zeek script against a pcap
- `taskset -pc 0 6193`
- LOOK FOR MULTIPLE VICTIMS



# MOD 30
## DDOS
### 




## Brute Force
- Utilize the message field, looking for "*Failed Log On*" or "*Failed Password*"

```
GET /_search
{
  "query":{
   "match": {
    "FIELD": "TEXT"
   }
  }
}
```
