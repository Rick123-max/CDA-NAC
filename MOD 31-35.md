- token=sessid=cookie (DIFFERENT SOUCRE IP's USING SAME TOKEN)
- `zeek -r traffic.pcap script.zeek` to run a zeek script against a pcap
- `taskset -pc 0 6193` for efficiency -p for PID and -c for CPU 
- LOOK FOR MULTIPLE VICTIMS
- Search for ja3 in wireshark for fingerprints (either ja3 for client or ja3s for serverHello)
- 


# MOD 30
## DDOS
- Look for a lot of packets trying to overwhelm a system
- Also look for Syn/SynAck but not Ack
### 




## Brute Force
- Utilize the message field, looking for "*Failed Logon*" or "*Failed Password*"
- Look for `PRE_AUTH_FAILED`






```
GET /_search
{
  "query":{
   "match": {
    "FIELD": "TEXT"
   }
  }
}
```
